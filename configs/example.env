# Copy this file to .env in the project root (or set env vars in Windows).

# Path to the whitelist file (plates -> role)
GATEWATCH_WHITELIST_PATH=configs/whitelist.json

# SQLite event store (created if missing)
GATEWATCH_DB_PATH=data/gatewatch.db

# Live pipeline loop (off by default; /simulate always works)
# GATEWATCH_ENABLE_LIVE_PIPELINE=1
# GATEWATCH_TICK_INTERVAL_MS=100

# Camera source: "0" (default webcam), "1" (2nd cam), or an RTSP/HTTP/video-file path.
# GATEWATCH_CAMERA_SOURCE=0
# GATEWATCH_CAMERA_ID=gate-1

# Object detection (Ultralytics YOLO)
# GATEWATCH_YOLO_WEIGHTS=yolov8n.pt
# GATEWATCH_DET_CONF=0.25

# Optional license-plate recognition (disabled unless you provide a plate detector weights file)
# GATEWATCH_PLATE_DET_WEIGHTS=path/to/plate-detector.pt
# GATEWATCH_PLATE_DET_CONF=0.25
# GATEWATCH_OCR_LANGS=en

# Notifications
# Webhook URL for notifying reception dashboard
# GATEWATCH_WEBHOOK_URL=https://example.com/reception/webhook

# WhatsApp (via Twilio) - requires Twilio credentials.
# For WhatsApp, the "from" number usually looks like: whatsapp:+14155238886
# GATEWATCH_TWILIO_ACCOUNT_SID=
# GATEWATCH_TWILIO_AUTH_TOKEN=
# GATEWATCH_TWILIO_WHATSAPP_FROM=whatsapp:+14155238886
# GATEWATCH_TWILIO_WHATSAPP_TO=whatsapp:+15551234567
# GATEWATCH_WHATSAPP_COOLDOWN_SEC=30

# Email (SMTP) - optional, but rate-limited to avoid spamming.
# GATEWATCH_SMTP_HOST=
# GATEWATCH_SMTP_PORT=587
# GATEWATCH_SMTP_USER=
# GATEWATCH_SMTP_PASS=
# GATEWATCH_EMAIL_FROM=
# GATEWATCH_EMAIL_TO=
# GATEWATCH_EMAIL_COOLDOWN_SEC=300
